{
  "name": "worklogs",
  "version": "1.0.0",
  "description": "worklogs",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "concurrently --kill-others \"gulp watch\" \"tsc --watch\" \"nodemon ./dist/server.js\"",
    "test": "tsc && tslint -c tslint.json --project tsconfig.json",
    "prepublish": "rimraf ./dist && gulp build && tsc",
    "start": "rimraf ./dist && gulp build && tsc && node ./dist/server.js"
  },
  "author": "Martin Zagora <zaggino@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@types/config": "0.0.29",
    "@types/koa": "^2.0.30",
    "@types/koa-static": "^2.0.19",
    "config": "^1.21.0",
    "gulp": "^3.9.1",
    "gulp-watch": "^4.3.9",
    "koa": "^2.0.0",
    "koa-static": "^3.0.0",
    "rimraf": "^2.5.4",
    "typescript": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^2.2.0",
    "nodemon": "^1.10.0",
    "tslint": "^3.14.0"
  }
}
